# Runtime and packaging use the same stable pins.
# OpenCV 5 removed the Caffe importer used by age/gender .caffemodel files.
# MediaPipe requires the contrib distribution; it already provides cv2, so do
# not install opencv-python alongside it.
opencv-contrib-python==4.10.0.84
numpy==1.26.4
pydantic==2.13.4
mediapipe==0.10.14
fastapi==0.128.8
uvicorn==0.39.0
websockets==15.0.1
openvino==2025.4.1
pytest==8.4.2
jsonschema==4.25.1
httpx==0.28.1
# Windows production discovery returns DirectShow moniker paths and the matching
# OpenCV capture index from one native enumeration boundary.
cv2-enumerate-cameras==1.1.16
# Vision only verifies daemon-issued Ed25519 maintenance capabilities.
cryptography==46.0.7
